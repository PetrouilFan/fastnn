[pytest]
norecursedirs = quick_transformer_test.py train_transformer.py bench_*.py generate_dataset.py
testpaths = tests
python_files = test_*.py
markers =
    slow: slow-running tests (e.g. benchmarks)
    integration: integration tests that exercise the full pipeline
